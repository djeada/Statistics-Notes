# Spatial Statistics Scripts

Runnable examples for [`notes/spatial_statistics`](../../notes/spatial_statistics/README.md).

The existing examples cover spatial autocorrelation, point processes, and kriging. `spatial_block_validation.py` adds an important assessment lesson: random cross-validation can be optimistic when nearby observations are dependent, so validation should respect spatial separation.
