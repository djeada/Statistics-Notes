# Resampling & Model Assessment Scripts

Runnable examples for [`notes/resampling_and_model_assessment`](../../notes/resampling_and_model_assessment/README.md).

This folder contains bootstrap/resampling examples, predictive metrics, outlier diagnostics, and `validation_and_model_selection.py`, which keeps model selection inside development data and reserves the test set for final evaluation.
