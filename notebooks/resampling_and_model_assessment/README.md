# Resampling & Model Assessment Notebooks

Interactive companions for [`notes/resampling_and_model_assessment`](../../notes/resampling_and_model_assessment/README.md). Use `resampling.ipynb` for bootstrap/permutation ideas, `metrics.ipynb` for predictive metrics, and `validation_and_model_selection.ipynb` for leakage-safe model selection.
