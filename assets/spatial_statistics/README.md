# Spatial Statistics Figures

These figures support the expanded spatial statistics notes. Each subdirectory corresponds to one chapter and is regenerated by the matching visualization script in [scripts/spatial_statistics/](../../scripts/spatial_statistics/README.md).

The scripts use deterministic seeds for their teaching examples and write outputs relative to the repository root, so they can be run from any working directory.
